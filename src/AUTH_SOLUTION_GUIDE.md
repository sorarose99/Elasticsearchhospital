# دليل حل مشاكل تسجيل الدخول - Hospital Management System

## المشكلة
النظام مُعد للعمل مع **Supabase** لكن حاليًا يعمل في **الوضع المحلي**. هناك تضارب بين نظامي المصادقة مما يسبب مشاكل في تسجيل الدخول.

## الحل المُطبق

### 1. نظام مصادقة متكيف (Adaptive Auth System)
تم إنشاء `useAdaptiveAuth` الذي يجمع بين:
- **Local Authentication**: للعمل المحلي مع حسابات تجريبية
- **Supabase Authentication**: للعمل مع قاعدة بيانات سحابية

### 2. واجهة تبديل الأنظمة (AuthSwitcher)
- واجهة مرئية لاختيار نظام المصادقة
- فحص حالة الاتصال بـ Supabase
- تبديل سهل بين الأنظمة
- عرض معلومات التكوين

### 3. عرض حالة النظام (AuthStatusDisplay)
- مؤشر صغير يظهر النظام الحالي
- فحص حالة الاتصال بـ Supabase
- زر سريع لتبديل الأنظمة

## كيفية الاستخدام

### للعمل المحلي (الافتراضي):
1. النظام يعمل حاليًا في الوضع المحلي
2. استخدم أي من الحسابات التجريبية العشرة المعروضة
3. اضغط على أي حساب للدخول مباشرة

### للانتقال إلى Supabase:
1. اضغط على "تبديل قاعدة البيانات" في شاشة تسجيل الدخول
2. ستظهر واجهة تبين حالة الاتصال بـ Supabase
3. إذا كان Supabase متاح، يمكنك التبديل إليه
4. إذا لم يكن متاح، ستحتاج لإعداد قاعدة البيانات أولاً

## حالة Supabase الحالية

### معلومات التكوين:
- **Project ID**: `uszizembzrusjhpoeibm`
- **URL**: `https://uszizembzrusjhpoeibm.supabase.co`
- **Status**: مُكوَّن لكن يحتاج إعداد قاعدة البيانات

### لتشغيل Supabase:
1. تأكد من إنشاء الجداول المطلوبة في قاعدة البيانات
2. قم بتشغيل migrations في مجلد `/supabase/migrations/`
3. أضف بيانات المستخدمين إلى Supabase
4. سيصبح النظام جاهزًا للتبديل

## الحسابات التجريبية المتاحة (النظام المحلي)

1. **مدير النظام**: `admin@clinic.com` / `admin123`
2. **طبيب**: `doctor@clinic.com` / `doctor123`
3. **ممرضة**: `nurse@clinic.com` / `nurse123`
4. **استقبال**: `receptionist@clinic.com` / `receptionist123`
5. **مختبر**: `lab@clinic.com` / `lab123`
6. **صيدلي**: `pharmacy@clinic.com` / `pharmacy123`
7. **أشعة**: `radiology@clinic.com` / `radiology123`
8. **فوترة**: `billing@clinic.com` / `billing123`
9. **موارد بشرية**: `hr@clinic.com` / `hr123`
10. **مخازن**: `inventory@clinic.com` / `inventory123`

## المميزات الجديدة

### 1. تبديل تلقائي:
- النظام يكتشف تلقائيًا نوع المصادقة المتاح
- يحفظ تفضيلات المستخدم
- يتذكر النظام المُختار

### 2. فحص الاتصال:
- فحص دوري لحالة Supabase
- عرض مؤشرات بصرية للحالة
- رسائل خطأ واضحة

### 3. تجربة مستخدم محسّنة:
- أزرار تبديل واضحة
- مؤشرات حالة
- رسائل مساعدة

## استكشاف الأخطاء

### إذا لم يعمل تسجيل الدخول:
1. تأكد من استخدام الحسابات التجريبية الصحيحة
2. اضغط على "تبديل قاعدة البيانات" لفحص الأنظمة المتاحة
3. جرب العودة للنظام المحلي إذا كان هناك مشكلة في Supabase

### إذا كان Supabase غير متاح:
1. تحقق من اتصال الإنترنت
2. تأكد من إعداد قاعدة البيانات
3. راجع إعدادات Supabase في ملف `info.tsx`

## الخطوات التالية

1. **للاستمرار مع النظام المحلي**: استخدم الحسابات التجريبية كما هي
2. **للانتقال لـ Supabase**: 
   - أعد قاعدة البيانات
   - انقل بيانات المستخدمين
   - استخدم واجهة التبديل

---

💡 **نصيحة**: النظام المحلي يعمل بشكل مثالي للتجريب والتطوير. يمكنك التبديل لـ Supabase لاحقًا عند الحاجة لمميزات قاعدة البيانات السحابية.